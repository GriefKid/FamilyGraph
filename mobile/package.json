{
  "name": "familygraph-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Android shell for the FamilyGraph Django application",
  "scripts": {
    "configure": "node scripts/configure-server.mjs",
    "sync": "npm run configure && npx cap sync",
    "add:android": "npx cap add android",
    "open:android": "npx cap open android",
    "build:android": "npx cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
